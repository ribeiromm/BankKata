@model IEnumerable<BankKata.Investiment.ShareDetails>

@{
    ViewBag.Title = "Check Share Details";
}

<h2>Check Share Details</h2>



<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CompanyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeRequested)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeRequested)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new {id = item.AccountTransactionsId}) |
                    @Html.ActionLink("Details", "Details", new {id = item.AccountTransactionsId}) |
                    @Html.ActionLink("Delete", "Delete", new {id = item.AccountTransactionsId})
                </td>*@
        </tr>
    }

</table>